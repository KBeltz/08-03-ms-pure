<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <% user = User.find(@shift.user_id) %>
  <%= user.username %>
</p>

<p>
  <strong>Location:</strong>
  <% location = Location.find(@shift.location_id) %>
  <%= location.location_name %>
</p>

<p>
  <strong>Weather:</strong>
  <% weather = Weather.find(@shift.weather_id) %>
  <%= weather.weather.capitalize %>
</p>

<p>
  <strong>Start Time:</strong>
  <%= @shift.start_time.strftime("%a, %b %d %Y, %I:%M %P") %>
</p>

<p>
  <strong>End Time:</strong>
  <%= @shift.end_time.strftime("%a, %b %d %Y, %I:%M %P") %>
</p>

<%= link_to 'Edit', edit_shift_path(@shift) %> |
<%= link_to 'Back', shifts_path %>
